#include<iostream>
#include<math.h>
using namespace std;
int main(){
	int base , power;
	cout<<"enter base";
	cin>>base;
	cout<<"enter power";
	cin>>power;
	int Expo = pow(base,power);
	cout<<"Exponential power is = "<<Expo;
	
}
